<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.conan.mapper.ReviewMapper">

	<!-- 관람평 db 저장 -->
	<insert id="write">
		insert into review (userID, userNickname, movieCd,movieNm, rate, content,imgUrl) 
		values(#{user.userID},#{user.userNickname},#{movieCd},#{movie.movieNm},#{rate},#{content},#{movie.imgUrl})
	</insert>
	

	<!-- 상세페이지 관람평 출력 -->
	<select id="getMovieReviews"
		resultType="com.conan.domain.Review">
		select userNickname, movieCD, movieNm, rate, content,imgUrl from review where movieCd = #{movieCd}
	</select>

	<!-- 상세페이지 movie db 출력 -->
	<select id="getMovieDetail" resultType="com.conan.domain.Movie">
		select * from movie where movieCd = #{movieCd}
	</select>
	
	<!-- 상세페이지 평균 평점 출력 -->
	<select id="getAvgRate" resultType="com.conan.domain.Movie">
		select avg(rate) from review where movieCd = #{movieCd}
	</select>

</mapper>

