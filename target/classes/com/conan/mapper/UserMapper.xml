<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.conan.mapper.UserMapper">
	<select id="getDate" resultType="string">
		select now()
	</select>

	<select id="login" resultType="com.conan.domain.User">
		select * from user where userID =
		#{userID}
	</select>

	<insert id="join">
		insert into user (userID, userPassword, userName,
		userNickname, userEmail)
		values(#{userID}, #{userPassword},
		#{userName}, #{userNickname}, #{userEmail})
	</insert>

	<select id="getAccount" resultType="com.conan.domain.User">
		select * from user where
		userID = #{userID}
	</select>

	<select id="getMyReviews" resultType="com.conan.domain.Review">
		select reviewID,
		userNickname,
		movieCd, movieNm, rate, content, imgUrl from review where
		userID =
		#{userID}
	</select>

	<select id="getMyFavorites"
		resultType="com.conan.domain.Favorite">
		select movieCd, movieNm, rate, imgUrl from favorite where
		userID = #{userID}
	</select>

	<!-- 회원 탈퇴 -->
	<delete id="deleteUser">
		delete from user where userID = #{userID} and
		userPassword = #{userPassword}
	</delete>

	<delete id="deleteReview">
		delete from review where reviewID = #{reviewID}
	</delete>

	<select id="getMyReview" resultType="com.conan.domain.Review">
		select reviewID,
		userNickname,
		movieCd, movieNm, rate, content, imgUrl from review where
		reviewID =
		#{reviewID}
	</select>

	<update id="modifyReview" parameterType="Map">
		UPDATE review
    	SET content = #{content}, rate = #{rate}
    	WHERE reviewID = #{reviewID}
	</update>
</mapper>